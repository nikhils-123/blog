
<%= form_with model: @part do |form| %>
	 
	<p>
		<%= form.label :Assembly_name %>
		<%= form.select :assembly_id, options_for_select(Assembly.all.map{|b| [b.name, b.id]}) %>
	</p>
	

	<p><%= form.submit %></p>
<%end%>

<p><%= link_to 'Back', part_path %></p>


